apiVersion: v1
kind: Secret
metadata:
  name: {{ include "logstash.fullname" . }}-env
  labels:
    {{- include "logstash.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- if .Values.env }}
  {{- toYaml .Values.env | nindent 2 }}
  {{- end }}
