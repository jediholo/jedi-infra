# Default values for jobs.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: ghcr.io/fab-infra/php
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: 7.4-opensuse15

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

nodeSelector:
  server-role-web: "true"

tolerations: []

affinity: {}

schedules: {}
  # certbot-renew: "0 2 * * *"
  # build-skinpack: "0 5 * * *"
  # jedi-skinpack: "0 5 * * *"
  # piwik-archive: "0 * * * *"
  # rpmod-web: "*/5 * * * *"
  # wordpress: "0 * * * *"

volumes:
  apps: {}
    # hostPath:
    #   path: /home/domains/example.org/apps
    #   type: DirectoryOrCreate
  subdomains: {}
    # hostPath:
    #   path: /home/domains/example.org/sd
    #   type: DirectoryOrCreate
  letsencrypt: {}
    # hostPath:
    #   path: /home/domains/example.org/letsencrypt
    #   type: DirectoryOrCreate
